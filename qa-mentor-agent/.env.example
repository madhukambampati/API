ANTHROPIC_API_KEY=your_api_key_here
CLAUDE_MODEL=claude-sonnet-5
PORT=5000

# Optional: set these to password-protect a public deployment (e.g. Vercel).
# Leave both unset for local use — the login gate stays disabled.
SITE_PASSWORD=
SECRET_KEY=
